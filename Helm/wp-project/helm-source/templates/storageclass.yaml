---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-{{ .Chart.Name }}
provisioner: cluster.local/nfs-nfs-subdir-external-provisioner
parameters:
  onDelete: "delete"
  pathPattern: "/project/wordpress/"
